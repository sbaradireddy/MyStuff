boto3>=1.34.0

# Image
Pillow>=10.0.0
imagehash>=4.3.1
opencv-python-headless>=4.9.0
pytesseract>=0.3.10        # optional local OCR fallback (needs system tesseract-ocr binary)

# Audio / Video (needs system ffmpeg + ffprobe binaries available on PATH)
openai-whisper>=20231117   # optional local ASR fallback if not using aws_transcribe

# General
numpy>=1.26.0
