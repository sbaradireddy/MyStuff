-- Check LKP_ADD_VEH_DUPLICATES results
SELECT * 
FROM LKP_ADD_VEH_DUPLICATES_cache 
WHERE POL_PK IN (83709650, 83499687, 83499688);

-- Check LKP_REINSTATE_WITH_LAPSE1 results
SELECT * 
FROM LKP_REINSTATE_WITH_LAPSE1_cache 
WHERE POL_PK IN (83709650, 83499687, 83499688);

-- Check LKP_CANCEL results
SELECT * 
FROM LKP_CANCEL_cache 
WHERE POL_PK IN (83709650, 83499687, 83499688);
