WITH Final AS (
    -- Replace FINAL_SELECT_S20_NJ_DMV_MTHLY_BUILD with your final query
    SELECT * FROM FINAL_SELECT_S20_NJ_DMV_MTHLY_BUILD
),
Source AS (
    SELECT * FROM S10_NJ_DMV_MTHLY_EXTRACT
)
SELECT *
FROM Final
WHERE NOT EXISTS (
    SELECT 1
    FROM Source
    WHERE Final.POL_PK = Source.POL_PK
      AND Final.POL_NUM = Source.POL_NUM
      AND Final.DMV_VIN = Source.DMV_VIN
      -- Add other key fields to match
);
